name: Build Windows Executable

on:
  push:
    branches: [ master ]
    tags: [ 'v*' ]
    paths-ignore:
      - 'LICENSE'
      - 'README.md'
      - 'README.zh_Hans.md'
      - 'README.zh_Hant.md'
      - 'assets/**'
  pull_request:
    branches: [ master ]
    paths-ignore:
      - 'LICENSE'
      - 'README.md'
      - 'README.zh_Hans.md'
      - 'README.zh_Hant.md'
      - 'assets/**'
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build executable
      run: |
        pyinstaller --onefile --windowed --name=WordDuplicateDetector --add-data "locales/*;locales" main.py

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: WordDuplicateDetector-Windows
        path: dist/WordDuplicateDetector.exe

  release:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build-windows
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          */WordDuplicateDetector.exe
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
